import React from 'react';
import { Placeholder } from '@/ui/Placeholder';

// Member detail — Phase 5. The list already links here so navigation is real.
export default function MemberDetail() {
  return <Placeholder title="Member detail" phase="Phase 5 (front desk)" />;
}
