/**
 * Re-export from the canonical types file.
 * All new code should import directly from '@/types'.
 * This file exists only for backward compatibility with existing pages.
 */
export * from '@/types';
