# Technical debt register

Items acknowledged but deliberately not fixed in the current slice. Each entry
names the blocker it creates and where it came from. Remove entries when paid.

| # | Item | Detail | Origin |
|---|---|---|---|
| ~~1~~ | ~~Stale auth spec DI providers~~ | **PAID 2026-08-04.** Fixed along with 29 other stale suites; backend suite is now 528 passing / 0 failing. | M0 Fix 1 → paid M3 |
| 3 | Phantom role names in `@Roles(...)` repo-wide | **Counted 2026-08-04: exactly 72 decorator sites across 19 controllers** referencing `'manager'` (58), `'admin'` (18) or `'staff'` (1) — none exist in `ENTERPRISE_ROLES` (real: `branch_manager`, `regional_manager`, `front_desk`, …). `RolesGuard` does **no** alias expansion, so only the `super_admin/owner/brand_owner` bypass saves these routes: a real `branch_manager` is blocked wherever `@Roles('manager')` guards a route with no matching permission path. Worst offenders: `marketing/automation.controller.ts` (13), `inventory/products.controller.ts` (12), `platform/controllers/platform.controller.ts` (7), `compliance/compliance.controller.ts` (7). M0 Fix 5 corrected `roles.controller.ts` only; the sweep needs a per-controller decision on which real roles belong. | M0 Fix 5 (2026-08-03), quantified 2026-08-04 |
| 2 | `analytics.view` is all-or-nothing | `/api/v1/analytics/*` has no per-role data scoping; one flat permission gates member behavior AND gym-wide revenue. Trainer was excluded from the grant for this reason (see rbac-seed comment). `marketing_manager` keeps `analytics.view` for campaign analytics but can therefore also call `/analytics/revenue` — acceptable-for-now decision, revisit if a finer split (`analytics.view_financial`) is wanted. | M0 Fix 1 (2026-08-03) |

| ~~4~~ | ~~Stale payments specs (constructor arity)~~ | **PAID 2026-08-04.** All payments suites green. | M0 Fix 9 → paid M3 |
| 5 | No public pricing API | The landing page's plan cards are hand-maintained in `frontend/src/app/landing/components/Pricing.tsx` and must be kept in sync with `backend/src/common/plan-configs.ts` by hand (they had drifted: Pro shown as ₹1,999 vs real ₹2,499, Enterprise as "Custom" vs ₹4,999, Free tier missing entirely). A small public `GET /api/v1/public/plans` would remove the drift class. | M0 Fix 11 (2026-08-03) |
| 6 | Base-PrismaService usage audit (post per-gym-schemas) | Services querying tenant models through `PrismaService` cannot see data in per-gym physical schemas — it resolves to `studio_template`. Fixed: check-in orchestrator + rules, qr.controller, report.processor. Remaining known site: `auth.service.ts:1742` (`prisma.staff.create` during onboarding) — left alone because onboarding runs before a tenant context exists and changing it touches the auth/provisioning path (HARD STOP #2). Re-audit with `grep -rn "this\.prisma\.\(member\|checkIn\|payment\|staff\)" src/` after the per-gym-schemas branch lands. | M3 E2E (2026-08-04) |
