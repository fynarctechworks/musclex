# Shared map for WebSocket upgrade. Installed to /etc/nginx/conf.d/ ONLY if no
# $connection_upgrade map already exists on the box (deploy.sh guards this).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
